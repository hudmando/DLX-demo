# reference/

Drop the working prototype here as **`prototype.html`** (the single-file three.js 0.161 build).

It is a **reference to extract from, not a file to edit in place.** The proven
`selectConnector` and derived Bill of Connectors are lifted into `src/engine/`; the three.js
scene becomes the seed for `src/view/`. See the root `CLAUDE.md`.
