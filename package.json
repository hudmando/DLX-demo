{
  "name": "dlx-field-planner",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Interactive 3D planner for DLX shelter camps. Centerpiece: the connection-validation engine that derives the Bill of Connectors.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "three": "0.161.0"
  },
  "devDependencies": {
    "@types/three": "0.161.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.6.0"
  }
}
